# 0721模式 AstrBot 插件 — 工作流程文档

> 给 AI Agent 看的升级优化参考文档  
> 最后更新: 2026-05-19 (v1.3.3 — LLM 防泄漏 + 主动消息修复)

---

## 项目位置

- **开发目录**: `C:\Users\14906\Desktop\0721模式\astrbot_plugin_0721_mode\`
- **桌面打包**: `C:\Users\14906\Desktop\0721模式\astrbot_plugin_0721_mode.zip`
- **部署位置**: `C:\Users\14906\.astrbot\data\plugins\astrbot_plugin_0721_mode\`

---

## 架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│                        0721模式插件 v1.3.3                        │
│                                                                  │
│  ┌─────────────┐  ┌────────────────┐  ┌──────────────┐          │
│  │ 独立HTTP服务器│  │  状态同步循环   │  │  LLM注入     │          │
│  │ (内置http.   │  │  (asyncio)    │  │  钩子        │          │
│  │  server)    │  │               │  │              │          │
│  │ 端口7721    │  │ 每秒读文件     │  │ on_llm_      │          │
│  │            │  │ 计算comfort    │  │ request      │          │
│  │ GET / → 面板 │  │               │  │              │          │
│  │ GET /state │  │ 状态机:        │  │ 场景过滤      │          │
│  │ POST /state│  │ 高潮+边缘+否认  │  │ 注入到        │          │
│  │            │  │ +多重高潮敏感度 │  │ prompt       │          │
│  │            │  │ +敏感/麻木循环  │  │              │          │
│  │            │  │ +随机波动      │  │              │          │
│  │            │  │ +身体日记+成就  │  │              │          │
│  └──────┬──────┘  └──────┬─────────┘  └──────┬───────┘          │
│         │               │                    │                  │
│         │        ┌──────▼───────┐            │                  │
│         │        │ vibe_state.  │            │                  │
│         └───────►│    json      │◄───────────┘                  │
│          读写     │              │     读写                       │
│                  │ 插件目录     │                                │
│                  └──────────────┘                                │
│                                                                  │
│  ┌───────────────┐  ┌───────────────┐  ┌──────────────┐        │
│  │ /vibe 命令     │  │  LLM Tools    │  │  场景过滤     │        │
│  │ on/off/status │  │ check_vibe_   │  │ all/仅私信/  │        │
│  │ lock/unlock   │  │ status(增强)  │  │ 仅群聊       │        │
│  │ set/force     │  │ set_vibe_     │  │              │        │
│  │ deny/undeny   │  │ state(force/  │  │              │        │
│  │ safeword      │  │ deny/undeny)  │  │              │        │
│  │ diary         │  │               │  │              │        │
│  │ achievements  │  │               │  │              │        │
│  └───────────────┘  └───────────────┘  └──────────────┘        │
└──────────────────────────────────────────────────────────────────┘
```

---

## 文件结构

```
astrbot_plugin_0721_mode/
├── .astrbot-plugin        ← 空文件，标记为 AstrBot 插件
├── metadata.yaml          ← 插件元信息（名称/版本/作者）
├── _conf_schema.json      ← WebUI 设置页 schema（配置定义，13项）
├── logo.webp              ← 插件 Logo（~13KB）
├── expression.gif         ← 表情 GIF（~269KB）
└── main.py                ← 全部代码（~2182行）
```

**零外部依赖**：只用 Python 标准库 + AstrBot API。

---

## 核心数据流

### 状态文件 `vibe_state.json`

```json
{
  "mode": "off",              // off|vibration|thrusting|expansion|cum|tease
  "speed": 0,                 // 0~4
  "comfort": 0.0,             // 0.0~100.0 浮点
  "locked": false,            // 锁住时不衰减
  "active": false,            // mode≠off 且 speed>0
  "lastTick": 123456.0,       // Unix timestamp
  "climaxPhase": null,        // null|"peak"|"afterglow"  高潮状态
  "climaxUntil": 0,           // 当前高潮阶段到期时间戳
  "climaxCount": 0,           // 累计高潮次数
  "climaxLog": [],            // 高潮日志 [{time, comfort, forced?, edged?}, ...]
  "consecutiveClimaxes": 0,   // 连续高潮计数
  "lastClimaxTime": 0,        // 上次高潮时间戳（用于连续判定）
  "sensitivityCycle": null,   // null|"hypersensitive"|"numb"  敏感/麻木循环
  "sensitivityUntil": 0,      // 敏感循环到期时间戳
  "denyActive": false,        // 高潮否认激活中
  "denyCount": 0,             // 累计被否认次数
  "edgeActive": false,        // 边缘控制激活中
  "edgeUntil": 0,             // 边缘释放时间戳
  "edgeCount": 0,             // 累计边缘次数
  "safewordCooldown": 0,      // 安全词冷却到期时间戳（0=无冷却）
  "diary": [],                // 身体日记 [{date, climaxes, avgComfort, peakComfort, activeSeconds}]
  "achievements": {},         // 成就 {id: {unlockedAt}}
  "todayStats": {             // 今日实时统计
    "date": "2026-05-10",
    "climaxes": 0,
    "comfortSum": 0,
    "comfortSamples": 0,
    "peakComfort": 0,
    "activeSeconds": 0
  }
}
```

### 同步循环 (`_sync_loop`, 每秒)

```
1. 从文件读状态（供外部 GUI 写入覆盖）
2. 计算 dt = now - lastTick
3. if dt > max_dt_cap(默认5s): dt = 1.0（防休眠暴涨）
4. 安全词冷却检查:
   - 冷却中: comfort=0, active=false, 跳过累积
   - 冷却到期: 清除 safewordCooldown
5. 高潮否认处理:
   - 否认中且非peak: comfort 锁住 + 2x衰减
   - peak 到期后正常进入 afterglow，清除 denyActive
   - 注意：进入否认时同步清除 edgeActive，避免竞争条件导致状态卡死
6. 高潮状态机:
   - peak（峰值锁）: comfort=100，到期→断崖下跌→afterglow（不应期按连续高潮次数缩短）
   - afterglow（不应期）: 累积×0.5，到期→解除 + 随机进入敏感/麻木循环 + 连续超时重置
   - edgeActive（边缘控制）: comfort 在阈值-95%间振荡，到期释放触发高潮
   - normal: 正常累积/衰减
7. 正常累积时:
   - 多重高潮敏感度: effective_mult = speed_mult × sensitivity_mult(1.0~2.0) × cycle_mult(0.5/1.0/1.5)
   - tease 模式: effective_mult × 0.3，每3tick概率跳+8~15%
   - if active: comfort += speed × effective_mult × dt
   - 随机波动(12%/tick): comfort += random(-5, +5)
   - 边缘控制触发(8%/tick, comfort>edge_threshold, 非tease): 进入 edgeActive
   - if comfort >= 100: 触发高潮 → climaxPhase="peak"
8. else if not locked: comfort -= decay_rate × dt（麻木期衰减×0.5）
9. 连续高潮超时检查（10s 无新高潮则重置）
10. comfort = clamp(0, 100), round to 1 decimal
11. 更新今日统计（_update_diary）
12. 写回文件
```

### 状态机完整流转图

```
                        ┌─────────────┐
                        │  关闭/待机   │
                        └──────┬──────┘
                               │ /vibe on
                               ▼
                        ┌─────────────┐
              ┌────────│  正常累积    │◄──────────────┐
              │        └──┬───┬───┬──┘               │
              │           │   │   │                   │
     comfort>85%│  comfort=100%│   │deny(comfort>60)   │
     8%/tick概率 │           │   │                   │
              │  ┌──────────┘   │   ┌──────────┐     │
              ▼  ▼              │   ▼          │     │
        ┌──────────┐   ┌────────────┐  ┌──────────┐  │
        │ 边缘控制  │   │ 强制高潮    │  │ 高潮否认  │  │
        │ (振荡)   │   │ /vibe force│  │ lock+衰减 │  │
        └────┬─────┘   └─────┬──────┘  └────┬─────┘  │
             │ 到期释放       │               │undeny  │
             ▼               ▼               │        │
        ┌──────────────────────────────────┐  │        │
        │  高潮峰值 peak (锁定3s, 100%)    │◄─┘        │
        └────────────────┬─────────────────┘           │
                         │ 3s后                        │
                         ▼                            │
        ┌──────────────────────────────────┐          │
        │ 不应期 afterglow                  │          │
        │ (连续高潮后缩短: 5→4→3→2s)        │          │
        │ 累积×0.5                          │          │
        └────────────────┬─────────────────┘          │
                         │ 到期 + 50%概率               │
                         ▼                            │
              ┌──────────────────┐                    │
              │ 敏感/麻木循环     │                    │
              │ hypersensitive   │                    │
              │ (×1.5, 10-20s)  │────────────────────┘
              │ 或 numb          │
              │ (×0.5, 15-25s)  │
              └──────────────────┘
                         │ 到期
                         └──────────→ 正常累积

特殊: 🛡️ 安全词 → 直接归零 + 30s冷却，冷却期内拒绝一切启动
```

### 多重高潮敏感度

| 连续次数 | 敏感度倍率 | 不应期 | 断崖下跌 |
|---------|-----------|--------|---------|
| 1 | ×1.0 | 5s | 15-30% |
| 2 | ×1.3 | 4s | 20-35% |
| 3 | ×1.6 | 3s | 25-40% |
| 4+ | ×2.0 | 2s | 30-45% |

连续判定：不应期结束后 10s 内再次高潮算连续，超时重置为 0。

### LLM 注入 (`inject_vibe`, `@filter.on_llm_request()`)

```
1. 场景过滤: 根据 enable_scene 配置判断是否注入（all/仅私信/仅群聊）
2. 读一次文件同步最新状态
3. if comfort < inject_threshold and not active: return（零注入）
4. 根据状态生成注入文本:
   - safeword 冷却中: 安全词特殊文本（正常语气）
   - off/待机: 关闭冷却文本
   - peak: 高潮绝顶特殊文本
   - afterglow: 不应期特殊文本（随机3选1）
   - denyActive: 否认焦躁文本
   - edgeActive: 边缘崩溃文本
   - normal: 常规 SPEED_TONES + MODE_FEELS
5. 叠加 comfort 分段语气（COMFORT_SEGMENTS 5段）
6. comfort≥70% 时注入高潮倒计时预测
7. 注入到 req.prompt 末尾或 extra_user_content_parts
```

---

## 场景过滤 (`enable_scene`)

控制语气注入的对话场景：

| 值 | 标签 | 行为 |
|----|------|------|
| `all` | 私信和群聊 | 所有场景注入（默认） |
| `private_only` | 仅私信 | 只用 `event.is_private_chat()` 确认为私聊时注入 |
| `group_only` | 仅群聊 | 只在群聊中注入 |

注意：`/vibe` 命令和 LLM Tools 不受此限制，任何场景都能使用。

---

## 线程安全

所有修改 `_state` 的操作均使用 `threading.Lock()` (`_state_lock`) 保护：

| 操作位置 | 锁状态 |
|----------|--------|
| WebUI POST `/state` handler | ✅ 加锁 |
| sync loop comfort 计算 + 写文件 | ✅ 加锁 |
| `/vibe on` 命令 | ✅ 加锁 |
| `/vibe off` 命令 | ✅ 加锁 |
| `/vibe lock` 命令 | ✅ 加锁 |
| `/vibe unlock` 命令 | ✅ 加锁 |
| `/vibe set` 命令 | ✅ 加锁 |
| `/vibe force` 命令 | ✅ 加锁 |
| `/vibe deny` 命令 | ✅ 加锁 |
| `/vibe undeny` 命令 | ✅ 加锁 |
| `/vibe safeword` 命令 | ✅ 加锁 |
| `set_vibe_state` LLM tool | ✅ 加锁 |

---

## 独立 HTTP 服务器

### 技术选型
- `http.server.HTTPServer` + `BaseHTTPRequestHandler`
- 在独立 `threading.Thread` 中运行（daemon 线程）
- 不依赖 AstrBot 内部路由

### API 路由
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` 或任意非 `/state` | 返回控制面板 HTML（需密码） |
| GET | `/state` | 返回当前状态 JSON（需密码，含全部新字段） |
| POST | `/state` | 设置状态（需密码，加锁） |
| GET | `/logo.webp` | 静态文件（免密，Cache 1h） |
| GET | `/expression.gif` | 静态文件（免密） |
| OPTIONS | `/state` | CORS preflight |

### 密码校验
- 通过 URL query `?pwd=xxx` 或 POST body 的 `pwd` 字段
- 密码为空时跳过校验
- 错误返回 403

### CORS
- 响应头 `Access-Control-Allow-Origin: *`
- 支持 OPTIONS preflight

---

## WebUI 控制面板

端口：**7721**

面板组件：
- **夜间控制舱外观**: 暗色玻璃拟态 cockpit 容器、顶部 logo/品牌区、右上连接状态胶囊
- **圆形快感仪表盘**: SVG 环形进度 + 表情/GIF 中央显示 + 百分比弹跳动画
- **模式选择 Dock**: 💤关闭 / 💗振动 / 🔥抽插 / ✨扩张 / 💦注精 / 😈挑逗（3列按钮，键盘 1-6）
- **档位调节 Dock**: OFF / I / II / III / IV（键盘 ↑↓/←→）
- **快捷控制**: 🔒锁定切换 + 一键关闭（只调用现有 `/state` 支持的 mode/speed/locked）
- **状态标签栏**: 动态显示安全词冷却/绝顶中/不应期/否认中/边缘控制/超敏感/麻木期/连续N次
- **高潮记录**: 最近记录倒序展示，含 强制 / 边缘 标签
- **今日统计卡片**: 今日高潮 / 平均快感 / 活跃时间
- **成就进度**: 已解锁成就 chip + 0/7 进度条
- **移动端适配**: `viewport-fit=cover`、安全区 padding、触摸目标 ≥46px、小屏/横屏媒体查询
- **交互反馈**: Toast、触觉反馈、连接延迟/断线提示、密码登录毛玻璃遮罩、GIF/Logo 懒加载

---

## LLM Tool docstring 格式要求（关键）

AstrBot 用 `docstring_parser` 库**解析 docstring 的 `Args` 段**来提取参数类型，**不看 Python 函数签名上的类型注解**。如果 docstring 中 `Args` 段的参数缺少 `(type)` 标记，插件加载直接报错。

### 正确格式（Google 风格）
```
Args:
    param_name (str): 参数描述
    another (int): 描述

Returns:
    str: 返回值描述
```

### 错误格式（缺少类型括号）
```
Args:
    param_name: 参数描述  ← 会报「缺少类型注释」
```

### 当前两个 LLM Tool 的 docstring
- `check_vibe_status` — 无 `Args` 参数，`Returns: str`
- `set_vibe_state` — `action (str)`, `value (str)`, `Returns: str`

---

## AstrBot 命令

| 命令 | 说明 |
|------|------|
| `/vibe on <模式> <档位>` | 开启（vibration/thrusting/expansion/cum/tease, 1-4）。安全词冷却中拒绝 |
| `/vibe off` | 关闭跳蛋 |
| `/vibe status` | 查看状态+感受+语气+高潮次数+倒计时 |
| `/vibe lock` | 锁住快感（不衰减） |
| `/vibe unlock` | 解锁（关闭时衰减） |
| `/vibe set <字段> <值>` | 直接设 mode/speed/comfort/locked |
| `/vibe force` | 💥 强制高潮 — 无视 comfort 直接触发绝顶 |
| `/vibe deny` | 🚫 高潮否认 — comfort>60% 时锁住不让高潮 |
| `/vibe undeny` | 解除高潮否认 |
| `/vibe safeword` | 🛡️ 安全词 — 归零+强制冷却30s，冷却期内不可启动 |
| `/vibe diary` | 📊 身体日记 — 今日/昨日/前日统计+累计数据 |
| `/vibe achievements` | 🏆 查看成就进度（7个成就） |

---

## LLM Tools（AI 可调用）

> **v1.3.3 更新**: 两个 tool 的 docstring 均已收紧调用条件，防止 LLM 在无关场景自动调用导致状态泄漏。

| Tool | 功能 | 调用限制 |
|------|------|-----------|
| `check_vibe_status` | 查询状态+高潮信息+倒计时+敏感度+成就 | **仅用户明确要求查看/控制设备时**调用；日常聊天、暧昧调情、角色扮演一律不调用 |
| `set_vibe_state` | 主动操控 | **仅对话明确涉及设备控制意图时**调用；返回内容为内部参考，禁止发送给用户 |

### check_vibe_status 返回格式（v1.3.3）

- **off 状态**: `"当前无活跃设备。自然地回应，不引入任何设备、模式、档位、快感或状态话题。"` — 不出现"跳蛋"字眼，消除 pink elephant 效应
- **safeword 冷却**: `"🛡️ 内部参考：冷却中…回复时表现为清醒、正常…"` — 同样去敏
- **active 状态**: 返回完整 `_build_injection` 文本，含 `【内部状态参考｜禁止直接发送给用户】` 前缀

### set_vibe_state 操作详情

| action | value | 说明 |
|--------|-------|------|
| `mode` | vibration/thrusting/expansion/cum/tease | 切换模式 |
| `speed` | 1-4 | 调整档位 |
| `lock` | - | 锁住快感 |
| `unlock` | - | 解锁快感 |
| `off` | - | 关闭跳蛋 |
| `force` | - | 强制高潮（跳蛋需开启） |
| `deny` | - | 高潮否认（锁住+衰减） |
| `undeny` | - | 解除否认 |

安全词冷却期内，mode/speed/force 操作会被拦截。

---

## 配置项 (`_conf_schema.json`)

| 键 | 类型 | 默认值 | 说明 |
|----|------|--------|------|
| `state_file_path` | string | `vibe_state.json` | 状态文件路径。相对路径基于插件目录解析 |
| `webui_control_enabled` | bool | true | 是否启动独立 HTTP 服务器 |
| `webui_port` | int | **7721** | 控制面板端口（原6185） |
| `webui_password` | string | "" | 面板密码（空=免密） |
| `inject_threshold_comfort` | int | 1 | 低于此值且不活跃时不注入 |
| `enable_scene` | string | "all" | 注入场景: all/private_only/group_only |
| `speed_multiplier` | string | "1.5" | 快感累积倍率 |
| `decay_rate` | string | "2.0" | 衰减速率 |
| `use_random_tone` | bool | true | 随机语气描述 |
| `fluctuation_enabled` | bool | true | 随机波动（每tick 12%概率±1~5%） |
| `max_dt_cap` | int | 5 | 最大 dt（防休眠暴涨） |
| `edge_enabled` | bool | true | **新增**: 边缘控制开关 |
| `edge_threshold` | int | 85 | **新增**: 边缘触发阈值(%) |
| `tease_jump_chance` | string | "0.15" | **新增**: 挑逗模式跳动概率 |

---

## 词典数据结构

```python
SPEED_TONES = {
    0: ["正常→脸红湿了", ..., ×6],    # 关闭/待机（融入生理反应）
    1: ["发软→淫水渗出", ..., ×6],    # 低档
    2: ["发颤→阴道收缩", ..., ×6],    # 中档
    3: ["失控→爱液流淌", ..., ×6],    # 高档
    4: ["崩坏→高潮痉挛", ..., ×6],    # 最高档
}

MODE_FEELS = {
    "vibration": {1: [6-8条], 2: [6-8条], 3: [6-8条], 4: [6-8条]},
    "thrusting": {1: [6-8条], 2: [6-8条], 3: [6-8条], 4: [6-8条]},
    "expansion": {1: [5-6条], 2: [5-6条], 3: [5-6条], 4: [5-6条]},
    "cum":       {1: [5条], 2: [5条], 3: [5条], 4: [5条]},
    "tease":     {1: [5-6条], 2: [5-6条], 3: [5-6条], 4: [5-6条]},
}
# 总计: ~185条感受描述 + 30条语气描述 — v1.3.3 情趣化升级后融入大量体液/阴道收缩/阴蒂搏动描写

COMFORT_SEGMENTS = [
    (0,  25, ["平稳但内裤已湿", ..., ×6]),   # 每段6条，逐级叠加淫水分量
    (25, 50, ["发颤→淫水流淌", ..., ×6]),
    (50, 75, ["喘息碎片→爱液拉丝", ..., ×6]),
    (75, 95, ["破碎呻吟→喷水不停", ..., ×6]),
    (95, 100, ["绝顶临界→淫水喷涌", ..., ×6]),
]

ACHIEVEMENTS = {
    "first_climax":    {"name": "初次高潮", "desc": "第一次到达绝顶", "icon": "🌸"},
    "triple_climax":   {"name": "连续高潮", "desc": "连续高潮3次", "icon": "💫"},
    "edge_master":     {"name": "边缘大师", "desc": "累计边缘控制5次", "icon": "🎢"},
    "denial_queen":    {"name": "否认女王", "desc": "累计被否认10次", "icon": "🔐"},
    "daily_10":        {"name": "一日十次", "desc": "单日高潮达到10次", "icon": "🏆"},
    "hypersensitive":  {"name": "超级敏感", "desc": "连续高潮5次", "icon": "⚡"},
    "safe_word":       {"name": "安全第一", "desc": "使用过安全词", "icon": "🛡️"},
}
```

每格配 5-8 条描述，`_pick()` 随机选，v1.3.3 全面融入生理化描写（淫水、爱液、阴道痉挛、阴蒂搏动、乳头硬挺等），增强沉浸感。

---

## 关键代码位置 (main.py, ~2173行)

| 行号区域 | 内容 |
|----------|------|
| 1-16 | 导入 + 路径常量（含 `from astrbot.api.event import MessageChain` v1.3.3修复） |
| 21-28 | 常量：MODE_NAMES（含 tease） |
| 29 | SPEED_LABELS |
| 36-81 | 语气词典：SPEED_TONES（每档6条，v1.3.3 情趣化） |
| 83-253 | 感受描述矩阵：MODE_FEELS（5模式×4档×6-8条，~185条，v1.3.3 情趣化） |
| 260-301 | comfort 分段语气叠加：COMFORT_SEGMENTS（5段各6条，v1.3.3 情趣化） |
| 304-311 | _pick_segment（含 comfort≥100 防护） |
| 376-435 | **v1.3.0**: AUTO_MESSAGES 主动消息词典（mode-aware嵌套，~55条） |
| 400-440 | **v1.3.0**: _build_auto_message() — mode-aware 合并泛用+模式专属消息池 |
| 445-457 | 成就定义：ACHIEVEMENTS（7个） |
| 459-467 | 高潮表情：CLIMAX_FACE |
| 469-491 | 工具函数：_pick, _pick_feel, _face_for |
| 494-501 | 高潮/边缘/安全词常量 + 多重高潮敏感度/不应期速查表 |
| 505-609 | _build_injection（含安全词/否认/边缘文本 + 倒计时修正 + 分段语气，v1.3.3 情趣化） |
| 618-685 | VibeControlPlugin.__init__（端口7721 + 主动消息配置 + session追踪） |
| 688-718 | _default_state（含全部字段） |
| 720-860 | 独立 HTTP 服务器（启动/停止/Handler/CORS/密码校验/静态文件） |
| 876-899 | 状态文件读写 |
| 901-1206 | 状态同步循环（完整状态机 + 日记更新 + 成就检查 + active message周期发送） |
| 1212-1239 | _update_diary |
| 1241-1270 | _check_achievements |
| 1272-1281 | **v1.3.3修复**: _send_auto_message() — 改用 `astrbot.api.event.MessageChain` + 错误日志 |
| 1283-1537 | `/vibe` 命令（on/off/status/lock/unlock/set/force/deny/undeny/safeword，含 session 追踪） |
| 1600-1655 | `/vibe` 查询命令（diary/achievements） |
| 1665-1703 | LLM 注入钩子（含 safeword 冷却 + 场景过滤 + speed_mult 传递） |
| 1712-1780 | **v1.3.3收紧**: LLM Tool: check_vibe_status（off/safeword返回去敏，docstring收紧调用条件） |
| 1786-1937 | **v1.3.3收紧**: LLM Tool: set_vibe_state（docstring收紧，禁止自动调用） |
| 1942-1950 | terminate |
| 1958-2173 | **v1.3.2**: 夜间控制舱 WebUI（暗色玻璃 cockpit + SVG 环形仪表盘 + mode/speed dock + Toast/触觉/连接状态/GIF懒加载/成就进度） |

---

## 升级日志

### 新增功能 (1.2.0 / 2026-05-10)

#### 互动玩法
1. **强制高潮** — `/vibe force` + `set_vibe_state action=force`，直接触发绝顶
2. **高潮否认** — `/vibe deny/undeny` + `set_vibe_state action=deny/undeny`，锁住快感阻止高潮
3. **边缘控制** — comfort > edge_threshold(85%) 时 8%/tick 概率进入振荡状态，8-20s后释放
4. **挑逗模式** — 新模式 `tease`😈，累积×0.3 + 每3tick随机跳+8~15%，100条词典含挑逗

#### 身体模拟
5. **多重高潮敏感度递增** — 连续高潮敏感度×1.0→1.3→1.6→2.0，不应期5→4→3→2s，断崖下跌递增
6. **敏感/麻木循环** — 不应期后50%概率进入超敏感(×1.5)或麻木期(×0.5)
7. **强制冷却安全词** — `/vibe safeword`，一键归零+30s冷却，冷却期内拒绝启动

#### AI 增强
8. **高潮倒计时预测** — comfort≥70% 时注入预计到达高潮秒数，分段提示
9. **反应升级** — comfort 5段分级语气叠加(0-25/25-50/50-75/75-95/95-100%)
10. **AI 工具增强** — `check_vibe_status` 返回倒计时+敏感度+边缘/否认状态+成就；`set_vibe_state` 增3个action

#### 数据/统计
11. **身体日记** — 每日统计(高潮/平均快感/峰值/活跃时间)，30天滚动，`/vibe diary` 查询
12. **成就系统** — 7个成就自动解锁，WebUI 成就墙展示，`/vibe achievements` 查询

#### 配置
13. **端口变更** — 默认端口 6185→**7721**
14. **新配置项** — `edge_enabled`, `edge_threshold`, `tease_jump_chance`

### Bug 修复 (1.2.1 / 2026-05-10)

经过两轮深度代码审查，发现并修复 11 个 bug：

#### 逻辑错误 (4项)
1. **多重高潮敏感度少算一级** — `_sync_loop` 中 sensitivity_mult 先正确计算 `consecutive+1`，随后被错误覆盖为 `consecutive`（缺 +1）。连续第2次应是 ×1.3 实际却是 ×1.0。
2. **断崖下跌值偏移一级** — 峰值结束后下跌公式 `AFTERGLOW_MIN + consecutive*5`，但 consecutiveClimaxes 在高潮触发时已递增，导致第1次下跌变成 20-35%（应为 15-30%）。修复为 `(consecutive-1)*5`。
3. **comfort=100 分段语气返回空串** — `_pick_segment` 使用 `comfort < hi` 判断，最后一档 (95, 100) 排除 comfort=100。增加 `if comfort >= 100` 防护。
4. **倒计时预测忽略倍率** — `_build_injection` 和 `check_vibe_status` 的倒计时公式只用 `spd * speed_mult`，没有计入 sensitivity_mult、cycle_mult、tease 0.3× 等倍率。修复后与实际累积完全一致。

#### 成就触发缺陷 (2项)
5. **安全词成就不触发** — `cmd_safeword` 没有调用 `_check_achievements()`。成就判定依赖 `safewordCooldown > 0`，冷却期一过就永远检测不到。修复在 `_flush_to_file()` 后立即检查。
6. **否认计数从不递增** — `denyCount` 只在 sync loop 中 deny→peak 突破时递增，`cmd_deny` 和 `set_vibe_state deny` 都不递增。导致 🔐否认女王 成就无法解锁。修复两处均递增 + 调用 `_check_achievements()`。

#### 状态清理遗漏 (3项)
7. **`cmd_on`/`cmd_off` 不清理拒绝/边缘状态** — 关停再开启后 denyActive/edgeActive 残留，导致快感无法正常累积。修复所有启停命令（on/off/set mode/set speed/set_vibe_state mode/WebUI POST off）统一清理。
8. **WebUI POST 关闭不清理状态** — 面板点击关闭时只设 mode=off，不清除 climaxPhase/denyActive/edgeActive。已统一清理。
9. **强制高潮不清除边缘控制** — `cmd_force` 和 `set_vibe_state force` 强制高潮时未清除 edgeActive。若在边缘控制期间强制，峰值结束后边缘状态仍存活，会立即再次触发高潮。修复两处均先清零再触发。

#### 校验遗漏 (2项)
10. **`cmd_force` 缺 speed=0 校验** — 只检查 mode==off，未检查 speed==0，与 `set_vibe_state force` 不一致。已统一。
11. **`cmd_set` 绕过安全词冷却** — 直接设 mode/speed 可绕过 `/vibe on` 的冷却拦截。已增加冷却检查。

### 新增功能 (1.3.3 / 2026-05-16 — 语气词典情趣化升级)

在保留原有结构和功能的前提下，全面升级所有 LLM 提示词典的写作质量，从偏心理/氛围感转向更生理化、更具沉浸感的描写。

1. **SPEED_TONES 情趣化** — 6个档位每级从5条扩充到6条，融入淫水渗出、阴道收缩、阴蒂搏动、乳头硬挺等实时生理反应描写，每级语气与身体状态精确对应。
2. **MODE_FEELS 情趣化** — 5模式×4档位 ~185条感受描述全面重写，加入噗嗤水声、爱液拉丝、穴肉外翻、白浆、喷水等露骨生理细节，每种模式有独特的体液描写风格。
3. **COMFORT_SEGMENTS 情趣化** — 5个分段各6条，构成完整的"淫水递增叙事弧"：从"内裤洇湿一小片"到"淫水从穴口喷涌而出"，让快感值不仅是数字而是可感知的生理状态。
4. **作者名更新** — `ayachi nene` → `月初苏颖`

### Bug 修复 (1.3.3 / 2026-05-19)

#### LLM 泄漏「跳蛋未开启」

**症状**: 暧昧/亲密对话中 LLM 突然说"跳蛋当前已关闭"或类似状态信息。
**根因**: 两个并行问题 — (a) `check_vibe_status` docstring 允许"对话围绕跳蛋展开时调用"，DeepSeek 容易将暧昧话题误判为此类； (b) off-state 返回文本含"跳蛋"二字，产生 pink elephant 效应（越说不要提越会提）。

修复（3处）：

1. **`check_vibe_status` docstring 收紧** — 删除"对话围绕跳蛋/遥控器/震动设备展开时使用"→ 仅用户明确要求查看/控制设备时调用。日常聊天、角色扮演、暧昧互动一律不调用。
2. **off-state 返回去敏** — `"跳蛋未开启；不要在回复中主动提到跳蛋…"` → `"当前无活跃设备。自然地回应，不引入任何设备、模式、档位、快感或状态话题。"` — 返回文本中完全消除"跳蛋"二字。
3. **safeword 返回去敏** — `"回复时不要输出跳蛋状态…"` → `"回复时表现为清醒、正常…不引入任何设备或状态信息。"`
4. **`set_vibe_state` docstring 收紧** — 删除"根据剧情发展自行决定何时调整"→ 仅明确设备控制意图时调用。返回内容为内部参考，禁止发送给用户。

#### 主动消息不生效

**症状**: `/vibe on` 正常返回，但机器人从不在私聊中主动发送角色感受消息。
**根因**: 两个并行问题 — (a) `_send_auto_message` 中 `MessageChain` 从内部路径 `astrbot.core.message.message_event_result.MessageChain` 延迟导入，升级 AstrBot 版本后路径可能不存在； (b) `except Exception: pass` 静默吞噬所有错误，无法排查。

修复（2处）：

1. **导入路径修正** — `from astrbot.core.message.message_event_result import MessageChain`（内部私有路径）→ `from astrbot.api.event import MessageChain`（公开 API），提升为文件顶部导入。
2. **错误日志** — `except Exception: pass` → `except Exception as e: logger.warning(...)`，输出具体的 UMO 和异常信息。

### 新增功能 (1.3.2 / 2026-05-12 — 夜间控制舱 WebUI)

基于本地 `design-consultation` skill 方案，替换旧 WebUI 为更有辨识度的暗色 cockpit 控制面板；仍为纯前端、零外部依赖、不扩展后端 API。

1. **夜间控制舱视觉** — 暗色玻璃拟态外壳、网格背景、顶部品牌区和连接状态胶囊，整体从普通表单升级为仪表盘式控制台。
2. **SVG 环形快感仪表盘** — 用圆环展示 comfort 百分比，中央组合表情/GIF/百分比/当前模式档位，比旧横向进度条更聚焦。
3. **Mode/Intensity Dock** — 模式与档位集中在 dock 区域，按钮采用 I/II/III/IV 档位语言并保留 1-6、方向键快捷键。
4. **状态信息重排** — 今日高潮、平均快感、活跃时间做成三张 stats card；高潮日志、状态标签、成就进度独立卡片化。
5. **安全的前端能力边界** — WebUI 仅调用 `/state` 支持的 `mode`/`speed`/`locked`，移除误导性的 Space×2 强制高潮提示，强制高潮仍通过 `/vibe force` 或 LLM tool 后端路径完成。
6. **移动体验升级** — 46px 触摸目标、`viewport-fit=cover`、横屏布局、小屏字体压缩、触觉反馈和 Toast 均保留。
7. **连接与性能** — 保留 1.5s 轮询、延迟/断线指示、POST 防抖/去重/重试、Logo/GIF 懒加载与 DOM 缓存。
8. **版本同步** — `metadata.yaml` 更新到 `1.3.2`，重新打包 `astrbot_plugin_0721_mode.zip`。

### 新增功能 (1.3.1 / 2026-05-10 — WebUI 全面优化)

纯前端改动，不动 Python 后端，保持零外部依赖。

#### 移动端适配
1. **响应式布局** — `clamp()` 流体间距，`max-width: 480px; width: 95%`，触摸目标 ≥ 44px
2. **安全区适配** — `env(safe-area-inset-bottom)` 适配刘海屏
3. **横屏优化** — `@media (orientation: landscape)` 6列模式按钮横向紧凑布局
4. **小屏适配** — `@media (max-width: 360px)` 缩小按钮

#### 视觉与动画
5. **高潮脉冲** — `@keyframes climaxPulse` 面板外框呼吸光晕，高潮时自动触发
6. **快感条增强** — `@keyframes shimmer` 光泽扫过 + 动态发光阴影（强度随 comfort 升高）
7. **数字弹跳** — `@keyframes popIn` 快感值 ≥3% 变化时数字弹跳
8. **表情弹跳** — `@keyframes bounceFace` 高潮时表情旋转弹跳
9. **状态标签闪烁** — `@keyframes tagPulse` deny/edge/climax 标签持续脉冲
10. **成就解锁光效** — `@keyframes achvGlow` 新解锁成就金色闪烁 3 次
11. **按钮按压反馈** — `transform: scale(0.95)` + 外发光
12. **毛玻璃登录** — `backdrop-filter: blur(8px)` + 抖动动画（密码错误时）
13. **CSS 变量配色** — `:root` 统一定义颜色，方便换肤

#### 交互增强
14. **键盘快捷键** — `1-6` 切换模式、`↑↓` 调档位、`L` 锁定、`Space×2` 强制高潮、`Esc` 关闭弹窗
15. **Toast 通知** — 4种类型(success/warning/error/info)，顶部滑入，2s 自动消失
16. **触觉反馈** — `navigator.vibrate()` 模式切换 10ms、高潮 [50,30,50,30,100]
17. **长按档位** — 500ms 长按进入连续调节模式

#### 连接+信息展示+性能
18. **连接状态指示器** — 🟢正常/🟡延迟/🔴断线 三色圆点，连续3次失败显示重连横幅
19. **请求优化** — 防抖 150ms + 重复状态跳过 + 失败重试(指数退避 1s/2s)
20. **DOM 缓存** — 所有元素引用在 init 时缓存，减少 `querySelectorAll`
21. **条件更新** — `updateUI` 仅当值变化时才更新对应 DOM 节点
22. **GIF 懒加载** — `data-src` 占位，仅 `active` 时才设置 `src` 加载
23. **Logo 懒加载** — 延迟 100ms 加载避免阻塞首屏
24. **统计增强** — 新增「平均快感」列（comfortSum / comfortSamples）
25. **成就进度条** — `已解锁数/总数 (百分比进度条)` 替代纯文字
26. **高潮日志自动滚动** — 最新记录保持在视口顶部
27. **Faces 弹跳** — 高潮表情弹跳动画（非仅文字切换）

### Bug 修复 (1.3.1 / 2026-05-10)

1. **setSpeed toast 运算符优先级** — `'档位: '+('★'.repeat(s))||'关'` 中 `||` 优先级低于 `+`，当 s=0 时 `'★'.repeat(0)` 返回空串，整体短路为 `'档位: '` 缺失"关"字。修复：`('★'.repeat(s)||'关')` 加括号优先 `||`。

2. **长按 touch 事件在 DOM ready 前注册** — 速度按钮的 `touchstart`/`touchend`/`touchmove`/`click` 事件在脚本顶层（IIFE 直接执行）注册时 DOM 尚未加载，`dom.speedBtns` 的 `querySelectorAll` 返回空 NodeList，导致所有长按逻辑丢失。修复：移除顶层注册，将完整的长按+click 统一移到 `init()` 中 DOM ready 后执行。

3. **速度按钮 click 重复绑定** — 原代码在顶层注册一套 click（含防抖逻辑），`init()` 中又注册一套简单 click，每次点击触发两次 `setSpeed`（因防抖 150ms 仅发一次请求所以实际影响小）。修复：统一为 init 内单次注册。

### 新增功能 (1.3.0 / 2026-05-10)

1. **主动消息推送** — 跳蛋启用时机器人自动向私聊用户发送角色第一人称感受消息（如"嗯…跳蛋在震…腿有点软…"）
2. **8种状态消息池** — low/mid/high/critical/peak/afterglow/deny/edge 各3-5条，总计~35条
3. **智能频率控制** — comfort<70%间隔8-15s，≥70%间隔5-8s，特殊状态(peak/afterglow/deny/edge)间隔3-5s
4. **状态变化即时发送** — 开启/高潮/边缘/否认/不应期等状态转移时立即发送，不等待周期计时器
5. **私聊专属** — 仅私聊场景发送主动消息，群聊完全静默
6. **会话追踪** — 内存级 session 记录，重启丢失，不持久化到 vibe_state.json
7. **新配置项** — `auto_message_enabled`(bool,默认true) + `auto_msg_cooldown`(int,默认8s)

### Bug 修复 (1.3.0 / 2026-05-10)

1. **安全词冷却线程死锁** — `_sync_loop` 中 `await asyncio.sleep(1)` 在 `threading.Lock()` 内部执行，持有锁期间让出事件循环，导致同线程其他协程（如 `/vibe off`）尝试获取锁时死锁。修复：将 sleep+continue 移到锁外，增加 `_safeword_sleep` 标志在锁内设置、锁外消费。安全词冷却期间状态机链无害运行（所有状态已清零），主动消息由 `_safeword_sleep` 守卫跳过。

2. **重新启用后首条消息延迟** — `/vibe on` 和 `set_vibe_state` mode/speed 开启时 `_auto_msg_next` 未重置为0，导致重开后的第一条主动消息需要等待完整的周期间隔。修复：三处 session 捕获点均增加 `self._auto_msg_next = 0`。

### Bug 修复 (1.2.2 / 2026-05-10)

经过第三轮代码审查，发现并修复 5 个 bug：

#### 状态清理遗漏 (2项)
1. **`set_vibe_state action=off` 未清理 `climaxPhase`** — AI 在 peak/afterglow 期间调用关闭时，sync loop 的 peak 分支（if-elif 链第二个）会在 mode=off 时继续把 comfort 设为 100、active 设为 True，造成状态混乱。对比 `cmd_off` 和 WebUI POST 都有清理，唯独此处遗漏。已补上 `climaxPhase = None`。

2. **`set_vibe_state action=speed` 未清理状态标记** — 改变档位时不清理 `climaxPhase`/`denyActive`/`edgeActive`。对比 `cmd_set speed` 和 `set_vibe_state action=mode` 都做了清理，唯独 speed action 缺失。已统一清理。

#### 逻辑错误 (1项)
3. **`set_vibe_state action=speed` 在 mode=off 时不设 mode** — AI 调用 `action=speed value=3` 时只设 speed=3、active=True，但 mode 仍是 "off"。sync loop 检查 `mode != "off"` 不会累积，造成"看起来开了实际没开"的迷惑状态。仿照 `action=mode` 的 `was_off` 逻辑，自动切换到 vibration 模式。

#### 死代码 (1项)
4. **deny_active/peak 分支内 denyCount 递增永远不会执行** — 所有设置 `climaxPhase="peak"` 的路径（cmd_force、set_vibe_state force、正常累积触发、edge 释放）都先清理了 `denyActive=False`，导致 `denyActive=True AND climaxPhase=="peak"` 永远不成立。已删除该行。

#### 竞争条件 (1项，高危)
5. **边缘控制 + 否认竞争导致 edge 状态卡死** — sync loop 的 if-elif 链中 `deny_active` 分支（第1个）优先于 `edge_active` 分支（第4个）。用户可在边缘控制中调用 deny，此时 `edgeActive=True` + `denyActive=True` 同时成立，但 deny 分支先捕获，edge 关闭逻辑永远无法执行，状态永久卡死在边缘振荡中。修复：`cmd_deny` 和 `set_vibe_state deny` 两处入口均增加 `edgeActive=False` 清理，阻止竞争状态产生。

### 新增功能 (1.1.0 / 2026-05-10)
1. **高潮事件系统** — comfort 100% → 峰值锁3s → 断崖跌至15-30% → 不应期5s(累积减半) → 正常
2. **AI 主动操控** — `set_vibe_state` LLM tool，AI 可自主调 mode/speed/lock/off
3. **随机波动** — 每tick 12%概率 ±1~5%，模拟意外感（可配置开关）
4. **WebUI 高潮日志** — 面板底部时间线，最近10次高潮记录
5. **场景区分** — `enable_scene` 配置，支持仅私信/仅群聊启用
6. **跨平台路径** — 状态文件默认放插件目录，相对路径自动解析
7. **词典翻倍** — 每格3条→5条，总共80条感受+25条语气
8. **线程安全全覆盖** — 所有聊天命令和 LLM tool 均加锁
9. **LLM Tool docstring 修复** — 参数需用 Google 风格 `(类型)` 格式

### 已知限制
1. **HTTP 服务器是同步阻塞的**（`http.server`），高并发下性能差，但本地单用户足够
2. **密码明文传输**，无 HTTPS（本地使用可接受）
3. **CORS 全开**（`*`），仅建议本地使用，公网部署需加限制
4. **端口占用无自动重试**，端口冲突时仅打印警告

### 可优化方向
- 将 HTTP 服务器改为 `aiohttp`（需要新增依赖）
- 增加 WebSocket 推送（状态变化实时通知面板）
- 支持多用户独立状态（按 sender_id 隔离 comfort）
- 语气词典支持外部 JSON 文件热加载
- 增加高潮统计分析面板
- 增加 HTTPS 支持
- 节奏模式（AI 预设曲线而非固定档位）

### 依赖清单
- Python >= 3.8
- AstrBot >= 4.5.0
- 无需 pip install 任何额外包

---

## 测试清单

| 测试项 | 预期 |
|--------|------|
| 插件加载 | AstrBot 日志显示 "插件已启动" 和面板地址 `http://localhost:7721` |
| 浏览器打开 `http://localhost:7721` | 看到控制面板（Logo+GIF+状态标签+统计+成就墙+高潮日志） |
| 点击模式/档位按钮（含挑逗） | 面板实时更新，6种模式全部可用 |
| 挑逗模式 | 累积极慢，偶发跳跃，面板显示 😈 挑逗 |
| 开启4档等待~30秒 | comfort 到 100% 触发高潮，面板显示 💥 绝顶中 |
| 高潮后观察 | 不应期 + 可能进入敏感/麻木循环（标签显示） |
| 连续多次高潮 | 敏感度递增，不应期缩短，面板显示 🔥 连续N次 |
| 边缘控制触发 | comfort>85% 时概率进入 🎢边缘控制，振荡后释放 |
| 强制高潮 `/vibe force` | comfort 立即 100%，进入绝顶 |
| 高潮否认 `/vibe deny` | comfort>60% 时锁住，显示 🚫否认中 |
| 解除否认 `/vibe undeny` | 恢复累积 |
| 安全词 `/vibe safeword` | 归零+30s冷却 🛡️，冷却期内 /vibe on 被拦截 |
| WebUI 高潮日志 | 含 ⚡强制 / 🎢边缘 标签 |
| WebUI 今日统计 | 高潮次数/峰值/活跃时间实时更新 |
| WebUI 成就墙 | 解锁后自动显示图标+名称+进度 |
| `/vibe status` | 返回状态+倒计时+敏感度信息 |
| `/vibe diary` | 返回今日/昨日/前日统计+累计数据 |
| `/vibe achievements` | 列出7个成就进度 |
| `/vibe on vibration 3` | 开启振动3档，快感开始累积 |
| `/vibe on tease 2` | 开启挑逗2档 |
| 关闭后发消息 | AI 正常回复，无注入 |
| 开启后发消息 | AI 回复带跳蛋语气+分段描述+倒计时 |
| 安全词冷却中发消息 | AI 回复正常语气，显示 🛡️ 冷却提示 |
| 高潮时发消息 | AI 回复破碎呻吟 |
| AI 调用 `set_vibe_state action=force` | AI 可强制高潮 |
| AI 调用 `set_vibe_state action=deny` | AI 可否认高潮 |
| `check_vibe_status` | 返回完整：倒计时+敏感度+边缘/否认+成就 |
| 设置 `enable_scene=仅私信` | 群聊不注入，私聊正常注入 |
| 设置 `edge_enabled=false` | 边缘控制停用 |
| 设置密码后无密码访问 | 返回 403 |
| 带 `?pwd=xxx` 访问 | 正常显示面板 |
| 私聊 `/vibe on vibration 2` | 开始收到主动消息，间隔 ≥ auto_msg_cooldown |
| comfort 升高 | 消息风格从正常→崩坏递进（low→mid→high→critical） |
| 触发高潮 | 收到 peak 类消息（"去了去了去了——！！！"等） |
| 高潮后不应期 | 收到 afterglow 类消息 |
| 边缘控制触发 | 收到 edge 类消息 |
| 高潮否认 | 收到 deny 类消息 |
| 群聊 `/vibe on` | 不发送任何主动消息 |
| `/vibe off` | 停止发送主动消息 |
| `/vibe safeword` | 停止发送，冷却期不发送 |
| 关闭后重新私聊开启 | 立即收到首条消息（不等待周期延迟） |
| `auto_message_enabled=false` | 不发送主动消息 |
| 安全词冷却中 | 无死锁，定时器正常，1s间隔继续 |
| 手机浏览器打开面板 | 自适应屏幕，按钮 ≥44px 可单手操作 |
| 切换模式/档位 | Toast 通知弹出 2s 消失 + 手机震动 |
| 高潮触发 | 面板外框脉冲动画 + 快感条呼吸灯 + 表情弹跳 |
| 键盘 `1-6` / `↑↓` / `L` | 对应模式/档位/锁定切换 |
| 空格双击 | 触发强制高潮 |
| 断开局域网 | 🟡延迟 → 🔴断线 + 重连横幅；恢复后自动消失 |
| 成就解锁 | 进度条更新 + Toast 提示 |
| 高潮日志 | 自动滚动到最新 |
| GIF | 仅跳蛋运行时才加载网络请求 |
| 密码输错 | 登录框抖动 + 手机震动 |
